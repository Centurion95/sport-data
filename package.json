{
  "name": "notes-mern",
  "version": "1.0.0",
  "description": "notes-mern",
  "main": "index.js",
  "scripts": {
    "server": "cd backend && npm install && npm start",
    "client": "cd frontend && npm install && npm start",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server-dev": "cd backend && npm install && npm run dev",
    "client-dev": "cd frontend && npm install && npm run dev",
    "dev": "concurrently \"npm run server-dev\" \"npm run client-dev\""
  },
  "keywords": [],
  "author": "Rodrigo Centurión",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}